# 1. Soubory pro tvorbu JavaFXML Application

Používá se přístup FXML + Scene Builder:

## FXML soubory (.fxml)
- speciální XML formát používaný v JavaFX pro definici uživatelského rozhraní.
- definují vzhled aplikace (XML)
- vytvářeny často ve Scene Builderu
- Umožňuje oddělit vzhled (UI) od logiky (Java)
- UI se zapisuje deklarativně (bez programování v Javě)
- Používá se spolu s třídou FXMLLoader
- Může být propojen s controllerem pomocí fx:controller

Příklad FXML:

```XML
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.VBox?>

<VBox spacing="10" alignment="CENTER"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.Controller">

    <Label text="Zadej jméno:" />

    <TextField fx:id="nameField" />

    <Button text="Potvrdit" onAction="#handleClick" />

    <Label fx:id="outputLabel" />

</VBox>
```

Načtení FXML:

```Java
FXMLLoader loader = new FXMLLoader(getClass().getResource("main.fxml"));
Parent root = loader.load();

Scene scene = new Scene(root);
stage.setScene(scene);
stage.show();
```

## Controller
- Java třída napojená přes FXML skrze fx:controller
- obsahuje logiku a reakce na události
- načtená při načtení FXML souboru pomocí `FXMLLoader`
- Obsahuje logiku pro prvky definované ve FXML
- Propojení přes fx:id a anotaci @FXML

```Java
@FXML
private Button myButton;

@FXML
private void handleClick() {
    System.out.println("Kliknuto!");
}
```

## Java hlavní třída
- načítá FXML pomocí `FXMLLoader`, dědí z `Application`

Třída Application je základ každé JavaFX aplikace.
Každá JavaFX aplikace od ní dědí
Slouží jako vstupní bod programu

Hlavní kroky:
- Spuštění pomocí launch(args)
- Automatické zavolání metody start(Stage stage)
- Při spuštění aplikace je metodě start zpřístupněna `Stage`
- V metodě start se vytváří GUI

```Java
public class Main extends Application {
    @Override
    public void start(Stage stage) {
        stage.setTitle("Moje aplikace");
        stage.show();
    }

    public static void main(String[] args) {
        launch(args);
    }
}
```

## CSS soubory
- Slouží ke stylování aplikace (barvy, fonty, velikosti)

# 2. Prostředí Scene Builder

Scene Builder je nástroj pro vizuální návrh GUI.

## Hlavní vlastnosti:
- drag & drop komponent
- generuje FXML kód automaticky
- náhled UI v reálném čase
- možnost nastavení vlastností (properties)

### Části prostředí:
- Library – seznam komponent
- Hierarchy – strom prvků
- Inspector – vlastnosti komponent
- Preview – náhled aplikace

# 3. Práce s komponentami (FXML + Controller)

FXML příklad:
```XML
<VBox spacing="10" alignment="CENTER"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.Controller">

    <TextField fx:id="nameField" />
    <Button text="OK" onAction="#handleClick"/>
    <Label fx:id="outputLabel"/>

</VBox>
```

Controller:
```Java
@FXML
private TextField nameField;

@FXML
private Label outputLabel;

@FXML
private void handleClick() {
    outputLabel.setText("Ahoj " + nameField.getText());
}
```

Načtení FXML:
```Java
FXMLLoader loader = new FXMLLoader(getClass().getResource("main.fxml"));
Parent root = loader.load();

stage.setScene(new Scene(root));
stage.show();
```

propojení pomocí:
`fx:id` → propojí prvek s controllerem
`@FXML` → umožní přístup k prvkům

události ve FXML:
`onAction="#metoda"`
