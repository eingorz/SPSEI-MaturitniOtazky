 - Co dělá?
	 - Vstupem je texťák
	 - Maže komentáře
	 - porovnává makra 
	 - Hlavičkové soubory
		 - .h
		 - Obsahuje definice funkcí
		 - začíná vždy podmíněným překladem
			 - Aby se soubor neincludenul 2x
			 - `` #ifndef MAKRO # DEFINE MAKRO``
			 - Definice vlastních typů
			 - Globální proměnné
			 - deklarace & definice funkcí
			 - ``#endif``
	 -  podmíněný překlad
		 - řízený existencí makra
			 - ifdef, ifndef
		 - řízený hodnotou makra 
			 - self explanatory, if MAKRO == hodnota
			 - elif MAKRO == jinahodnota
			 - endif
	 - Rozvíjí makra
```C
#define MAKRO 5
#define DALSIMAKRO printf("go kys");
int main(){
	int a = MAKRO // a = 5
	DALSIMAKRO // da se tady ten printf i se strednikem, btw strednik se muze dat tady i rovnou k definici makra, je to jedno
}

#define soucet(a,b) a + b
// idk nechapu pointu 
printf(soucet(a,b)); // printf(a + b)
printf(soucet(a+b,b)) //printf(a+b+b)

#define vypis(d,h,b){\for(;d<h;d++){\if(d%b!=0) printf("%d",d);)}}
vypis(1,5,2)
----
for(;1<5;1++) // -> nebude fungovat logicky lol, musi se tam pridat nejaka ridici pomena az v te kodove casti toho makra a pouzit to misto te promenne co mame jako argument protoze ten tam vlozi tak jak ho tam dame proste
```
- Oddělený překlad
	- Kompilátor + Linker
```
#DEFINE MAX (a>b) ? (a) : (b)
i = 5, j = 2;
res = MAX(++i,j)
res = 7
(++i) ? (++i) : (j)
```